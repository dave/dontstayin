
namespace Script.Contrib.jQueryUI
{
	using System.Collections;

	public partial class jQueryUIObject
	{
		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <returns>A instance of <see cref="jQueryUIObject"/>.</returns>
		public jQueryUIObject Button()
		{
			return null;
		}
		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <returns>A instance of <see cref="jQueryUIObject"/>.</returns>
		public jQueryUIObject Buttonset()
		{
			return null;
		}

		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <param name="options">http://jqueryui.com/demos/button/#options</param>
		/// <returns>A instance of <see cref="jQueryUIObject"/>.</returns>
		public jQueryUIObject Button(Dictionary options)
		{
			return null;
		}

		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <param name="method">http://jqueryui.com/demos/button/#methods</param>
		/// <returns>A instance of <see cref="jQueryUIObject"/>.</returns>
		public jQueryUIObject Button(string method)
		{
			return null;
		}

		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <param name="option">Must be "option" (http://jqueryui.com/demos/button/#methods).</param>
		/// <param name="optionName">http://jqueryui.com/demos/button/#options</param>
		/// <returns>The value of the option.</returns>
		public object Button(string option, string optionName)
		{
			return null;
		}

		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <param name="option">Must be "option" (http://jqueryui.com/demos/button/#methods).</param>
		/// <param name="optionName">http://jqueryui.com/demos/button/#options</param>
		/// <param name="value">Value to set for the option.</param>
		/// <returns>A instance of <see cref="jQueryUIObject"/>.</returns>
		public jQueryUIObject Button(string option, string optionName, object value)
		{
			return null;
		}

		/// <summary>
		/// Button enhances standard form elements like button, input of type submit or reset or anchors to themable 
		/// buttons with appropiate mouseover and active styles.
		/// In addition to basic push buttons, radio buttons and checkboxes (inputs of type radio and checkbox) can be converted to buttons: 
		/// Their associated label is styled to appear as the button, while the underlying input is updated on click.
		/// In order to group radio buttons, Button also provides an additional widget-method, called Buttonset. 
		/// Its used by selecting a container element (which contains the radio buttons) and calling buttonset(). 
		/// Buttonset will also provide visual grouping, and therefore should be used whenever you have a group of buttons. 
		/// It works by selecting all descendents and applying button() to them. You can enable and disable a buttonset, 
		/// which will enable and disable all contained buttons. Destroying a buttonset also calls the button's destroy method.
		/// When using an input of type button, submit or reset, support is limited to plain text labels with no icons.
		/// </summary>
		/// <param name="option">Must be "option" (http://jqueryui.com/demos/button/#methods).</param>
		/// <param name="options">Set multiple button options at once by providing an options object.</param>
		/// <returns>A instance of <see cref="jQueryUIObject"/>.</returns>
		public jQueryUIObject Button(string option, Dictionary options)
		{
			return null;
		}
	}
}
