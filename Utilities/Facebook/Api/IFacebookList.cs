using System;

namespace Facebook.Api
{
    /// <summary>An internal interface used as a type-agnostic marker to represent <see cref="FacebookList{T}" />.</summary>
    internal interface IFacebookList { }
}
